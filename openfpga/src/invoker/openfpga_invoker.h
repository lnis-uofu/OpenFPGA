/* header files required to declare the function */
#include <vector>
#include <string>

/*
function that will initialise all the objects and invoke the 
OpenFPGA depends upon the arguments given
*/

int mode_invoker(std::vector<std::string> &cmd_opts, const bool& tcl_mode);